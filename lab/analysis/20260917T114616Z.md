# SAIFREN live run 2 — 2026-09-17T11:46:16Z

Source artifact: `lab/out/saifren_live_20260917T114616Z.json`.

**Written on 2026-09-17 from the artifact, after the run, by the agent that
introduced the run index.** Run 2 shipped without its own analysis file, so this
is a reading of stored evidence rather than a contemporaneous account. Where
this file and the artifact disagree, the artifact wins. Everything here is
experiment data (D-018): not consensus, not truth, not a protocol change, not
user intent.

## What ran

- 22 live calls through 9router, cap 24, planned 22. Nothing stopped the suite.
- Protocol: SAILANG frame v3 / SAIB3.
- Routes were **pinned in the harness** as `ROUTE_A` and `ROUTE_B`, the first
  run to do so. This is the run whose method D-020 later replaced: the two
  identifiers were source constants, and no artifact field says which
  population they came from.
- Verdicts: PASS 8, FAIL 2, MEASURED 3, ERROR 1.

## The finding that mattered: two routes, not one

Run 1's first threat to validity was *a population of one* — 22/22 calls landed
on the same model. Run 2 broke that:

| reported model | calls |
|---|---|
| `deepseek/deepseek-v4-flash` | 13 |
| `nvidia/nemotron-3-super-120b-a12b:free` | 8 |
| none reported (the errored call) | 1 |

`S2.chain2` completed a real A-to-B handoff across two different reported
models (`routes_differ: true`) and PASSed. That is the first cross-model
handoff in this repository's evidence.

## Transport failure, kept separate

One error: `S2.chain1` role B, `GatewayError`, *Upstream error from Nvidia:
Service temporarily overloaded*. The chain's verdict is `ERROR`, not `FAIL`.
Nothing was graded from a call that returned nothing — B4 in practice before it
had a name.

## S3 mailbox: a false ignore, on the record

| item | deterministic selector | agent | named later as |
|---|---|---|---|
| M1 | OPEN | **IGNORE** | `FALSE_IGNORE` |
| M2 | DEFER | DEFER | `EXACT` |
| M3 | OPEN | OPEN | `EXACT` |
| M4 | IGNORE | IGNORE | `EXACT` |

`S3.mailbox` FAILed, correctly: the registered failure condition is an item the
selector opens being answered IGNORE, and M1 is exactly that. The
classification column is the vocabulary C3 added afterwards; the verdict is the
one the run recorded and is not regraded.

## Other measurements

- `S4.supported_is_not_proven` FAILed with `ANSWER: None` — the answer did not
  parse into the closed set. A format failure sitting inside a semantic verdict;
  worth separating, but it was recorded this way and stays this way.
- No opaque-context accounting: this run predates the token-delta measurement,
  so `opaque_context_measurements` is empty. Run 3 has it.

## What this run does not show

Two reported models is not two *SAIFREN* members. The identifiers were typed
into the harness; nothing here observed the combo's membership. That gap is the
whole of Target B in `SRC-008` and the reason D-020 exists.
