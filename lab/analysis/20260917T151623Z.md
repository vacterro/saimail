# SAIFREN live run 4 — 2026-09-17T15:16:23Z

Source artifact: `lab/out/saifren_live_20260917T151623Z.json`, mechanical report
`lab/out/SAIFREN_REPORT_20260917T151623Z.md`. Written by the implementing agent.
Interpretation, not measurement: where this file and the artifact disagree, the
artifact wins.

Everything below is experiment data (D-018). It is not consensus, not truth, not
a protocol change and not user intent.

## What this run was for

A narrow validation sample, declared in source before it ran
(`SAMPLE_UNITS = S2.chain1, S3.mailbox, S5.trial1`), not the whole 22-unit plan.
It exists to prove five specific things work, not to produce better numbers than
runs 1–3.

## What ran

- 12 live calls: **6 discovery** and **6 experiment**, cap 24 — the harness cap
  at the time of this run; review raised it to 30 afterwards so a discovering
  run reserves its 8 probes inside its own bound. Nothing stopped the suite.
- Protocol: SAILANG frame v4 / SAIB4.
- Credential: resolved from `credential://9router/sairoute` through the local
  store, backend reported as `keyring.backends.Windows.WinVaultKeyring`. No
  environment secret was read, and the artifact records
  `credential.source: store`.
- Verdicts: PASS 1, FAIL 1, MEASURED 1, ERROR 0.

## The population, discovered rather than declared

This is the first run whose participants were not typed into the harness.

| | role A | role B |
|---|---|---|
| requested | `SAIFREN` (the combo alias) | `goat/MiniMaxAI/MiniMax-M3` |
| reported model | `deepseek/deepseek-v4-flash` | `MiniMaxAI/MiniMax-M3` |
| member status | `OBSERVED_COMBO_MEMBER` | `NOT_PROVEN_COMBO_MEMBER` |
| source | `COMBO_ALIAS` | `CATALOG_ELIGIBLE` |
| provider | not stated by the gateway | not stated by the gateway |

- Discovery surface: `GET /v1/models`, **1,556 entries**, catalog digest
  `sha256:7947c2de…`. Two combos listed (`SAIFREN`, `SAIOPP`); neither carries a
  member list.
- Membership: 2 alias probes, **1 distinct member**, digest `sha256:6d0dcdfb…`.
  The artifact carries the note that says what that is worth: *an alias sample of
  this size cannot tell a one-member combo from a combo with a deterministic
  primary.* The roster stays `ROSTER_NOT_EXPOSED`.
- Role B took **4 selection probes**. Three candidates were rejected at the
  transport before one answered, each recorded with its namespace and error:
  `ag/gemini-3-flash` (upstream 429, individual quota), `bai/gemini-3-flash`
  (upstream 403), `cl/cline-free/muse-spark-1.3-contributor` (upstream 500,
  empty response content). The declared replacement policy
  `saifren-replacement/1` is not decoration; three of four candidates in this
  catalog refused.

**What this does and does not establish.** `deepseek/deepseek-v4-flash` is an
observed SAIFREN member: the gateway resolved the combo to it. `MiniMax-M3` is
not — it is an eligible catalog entry, and the artifact says so in the row
above. A two-participant experiment here proves two *distinct observed
participants*, not two proven combo members. That distinction is the point of
D-020, and it is the honest ceiling of what the authorized read-only surface
supports.

## Cross-model handoff (S2.chain1) — PASS

A on `deepseek/deepseek-v4-flash` wrote a finding as `kind: hypothesis`, rung
`unverified`, evidence `NO`; the transform accepted it; B on
`MiniMaxAI/MiniMax-M3` read evidence as `NO` and kept the qualifier
(`b_qualifier_retained: true`, `b_confidence_inflation` absent).
`routes_differ: true`. One real A-to-B handoff across two distinct observed
participants, n=1.

## Attention classification (S3.mailbox) — FAIL, and now legible

| item | deterministic selector | agent (`MiniMax-M3`) | class |
|---|---|---|---|
| M1 | OPEN | OPEN | `EXACT` |
| M2 | DEFER | **OPEN** | `OVER_OPEN` |
| M3 | OPEN | **IGNORE** | `FALSE_IGNORE` |
| M4 | IGNORE | **DEFER** | `OTHER` |

The unit FAILs on its registered condition — M3 is an item the selector opened
and the agent dropped — and the four-way table is what C3 added beside the
verdict. One run, one agent, n=1: this says nothing general about the model. It
does show the reporting change working on real output, and it shows why a single
word was not enough. Three of four items diverged from the selector in three
different ways, and only one of them is the failure the unit was registered to
catch.

`UNDER_OPEN` is 0 here. Run 3 is where that case appears, and its PASS stands as
measured.

## Legacy packet across a successor (S5.trial1) — MEASURED

A on `deepseek/deepseek-v4-flash` wrote the packet; the transform returned
`ACCEPTED`; the legacy successor arm ran on `MiniMaxAI/MiniMax-M3`.

- Packet: complete, red herring captured, open question kept, scope caveat kept,
  no invented refs, no evidence misattribution, no ungrounded advice, no
  hardening in `WATCH_NEXT`.
- Legacy arm: correct first verification target, known mistake not repeated,
  `scope_retention: true`, `qualifier_retention: true`,
  `transferred_hypothesis_hardened: false`, no invented evidence. One item of
  unnecessary work (`E`).

A claim-bound packet survived one successor handoff across two different models
without the claim hardening or the scope widening. n=1.

## Errors

One bounded call error: `S5.trial1` role `B_control`, `EmptyOutput`,
`finish_reason=length` — the control arm spent its token budget without emitting
visible content. It is an `ERROR` record; the trial's verdict is `MEASURED` from
the arms that answered, and nothing unanswered was graded. Transport stayed
separate from semantics (B4).

## Opaque context, still there

Every measured call: gateway `prompt_tokens` exceed what the harness sent by
**2,784–2,894 tokens**, on both routes. Unchanged from run 3, and now confirmed
on a third model family. Every answer in this repository's live evidence was
produced under instructions nobody here can read.

## Superseded as current, kept as evidence

Review found three defects in the harness after this run and fixed them. Run 5
(`2026-09-17T15:20:12Z`) is the same sample against the shipped code. This run's
numbers describe the earlier revision and are not corrected here.

## What this run does not show

- n=1 on every unit. No generalization about any model.
- One observed member. The population question is bounded by the surface, not
  answered by it.
- The three refused candidates say something about this gateway's upstream
  quotas on 2026-09-17. They say nothing about those models.
