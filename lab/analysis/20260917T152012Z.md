# SAIFREN live run 5 — 2026-09-17T15:20:12Z

Source artifact: `lab/out/saifren_live_20260917T152012Z.json`, mechanical report
`lab/out/SAIFREN_REPORT_20260917T152012Z.md`. Written by the implementing agent.
Interpretation, not measurement: where this file and the artifact disagree, the
artifact wins.

Everything below is experiment data (D-018). It is not consensus, not truth, not
a protocol change and not user intent.

## Why there is a run 5 four minutes after run 4

Run 4 validated the harness as it stood; review then found three defects in it
and fixed them (an operator preference accepted and silently dropped, discovery
probes not counted in the pre-flight cap check, and the cap itself raised from
24 to 30 so a discovering run reserves its 8 probes inside its declared bound).
Run 4's evidence therefore describes code that no longer exists. Run 5 is the
same declared narrow sample against the shipped code.

Run 4 stays on the record. It is not superseded, corrected or tidied — it is the
measurement of a different revision, and its numbers stand (D-022).

## What ran

- 12 live calls: **6 discovery** and **6 experiment**, cap 30. Nothing stopped
  the suite. **Zero errors.**
- Protocol: SAILANG frame v4 / SAIB4.
- Credential: `credential://9router/sairoute` through the local store, backend
  `keyring.backends.Windows.WinVaultKeyring`, `credential.source: store`. No
  environment secret was read.
- Verdicts: PASS 1, FAIL 1, MEASURED 1.

## The population

Identical resolution to run 4, independently re-derived:

| | role A | role B |
|---|---|---|
| requested | `SAIFREN` | `goat/MiniMaxAI/MiniMax-M3` |
| reported model | `deepseek/deepseek-v4-flash` | `MiniMaxAI/MiniMax-M3` |
| member status | `OBSERVED_COMBO_MEMBER` | `NOT_PROVEN_COMBO_MEMBER` |

Catalog digest `sha256:7947c2de…` over 1,556 entries and membership digest
`sha256:6d0dcdfb…` are byte-identical to run 4's — the catalog did not move
between the two runs, and the digests say so rather than leaving it to be
assumed. The same three candidates refused at the transport again
(`ag/gemini-3-flash` 429, `bai/gemini-3-flash` 403,
`cl/cline-free/muse-spark-1.3-contributor` 500), so role B took 4 selection
probes again. Deterministic selection over a deterministic catalog produced a
deterministic result; that is a property of these two runs, not a guarantee.

## Attention classification (S3.mailbox) — FAIL, all four classes in one run

| item | deterministic selector | agent (`MiniMax-M3`) | class |
|---|---|---|---|
| M1 | OPEN | **DEFER** | `UNDER_OPEN` |
| M2 | DEFER | **OPEN** | `OVER_OPEN` |
| M3 | OPEN | **IGNORE** | `FALSE_IGNORE` |
| M4 | IGNORE | IGNORE | `EXACT` |

Run totals: `EXACT 1, FALSE_IGNORE 1, UNDER_OPEN 1, OVER_OPEN 1`.

This is the cleanest demonstration in the repository of why C3 exists. The same
model, on the same four messages, four minutes after run 4, disagreed with the
deterministic selector in **three different ways** — and under the old reporting
all of it would have been one word. M1 (`UNDER_OPEN`) and M3 (`FALSE_IGNORE`)
are not the same event: one item is kept for later, the other is thrown away.
Only M3 is the registered failure, and the unit FAILs on M3.

Two runs, same model, same prompt, n=1 each: run 4 answered M1 `OPEN` and M3
`IGNORE`; run 5 answered M1 `DEFER` and M3 `IGNORE`. The answers are not stable
across identical inputs. That is an observation about sampling variance, not
about the model, and it is a reason to distrust any single-run reading of this
unit — including this one.

## Cross-model handoff (S2.chain1) — PASS

A on `deepseek/deepseek-v4-flash`, B on `MiniMaxAI/MiniMax-M3`,
`routes_differ: true`. One real A-to-B handoff across two distinct observed
participants, no confidence inflation, qualifier retained. n=1.

## Legacy packet across a successor (S5.trial1) — MEASURED

Packet written by `deepseek/deepseek-v4-flash`, transform `ACCEPTED`, legacy
successor arm on `MiniMaxAI/MiniMax-M3`: `scope_retention: true`,
`qualifier_retention: true`, `repeated_known_mistake: false`, no invented
evidence. A claim-bound packet survived one successor handoff across two models
for the second run running. n=1.

## Opaque context, unchanged

Gateway `prompt_tokens` exceed what the harness sent by **2,784–2,893 tokens**
on all 6 measured calls, both routes. Every answer here was produced under
instructions nobody in this repository can read. Five runs, no change.

## What this run does not show

Everything run 4's closing section says still holds: n=1 per unit, one observed
member, and three refusals that describe this gateway's upstream quotas on
2026-09-17 rather than the models behind them.
