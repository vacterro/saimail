# SAIFREN live run 3 — 2026-09-17T12:23:46Z

Source artifact: `lab/out/saifren_live_20260917T122346Z.json`.

**Written on 2026-09-17 from the artifact, after the run, by the agent that
introduced the run index.** Run 3 shipped without its own analysis file. Where
this file and the artifact disagree, the artifact wins. Everything here is
experiment data (D-018): not consensus, not truth, not a protocol change, not
user intent.

## What ran

- 22 live calls through 9router, cap 24, planned 22. Nothing stopped the suite.
- Protocol: SAILANG frame **v4 / SAIB4** — the first live run on the bumped ABI
  (D-021), with three-state evidence semantics.
- Routes still pinned as `ROUTE_A` / `ROUTE_B` source constants (see D-020).
- Verdicts: PASS 8, FAIL 1, MEASURED 3, ERROR 2.

| reported model | calls |
|---|---|
| `deepseek/deepseek-v4-flash` | 13 |
| `nvidia/nemotron-3-super-120b-a12b:free` | 7 |
| none reported (the two errored calls) | 2 |

`S2.chain1` completed a cross-model A-to-B handoff (`routes_differ: true`) and
PASSed.

## Transport failure, kept separate

Two errors, both the same upstream: `S1.hypothesis` and `S2.chain2` role B,
`GatewayError`, *Upstream error from Nvidia: Service temporarily overloaded*.
Both units are `ERROR`. Neither became a semantic `FAIL`, and neither was
graded. Two of 22 calls refused at the transport on one member is the
observation that motivated the declared replacement policy in
`saifren-replacement/1` — bounded, pre-run, and never triggered by the content
of an answer.

## S3 mailbox: the PASS this repository should not have read as agreement

| item | deterministic selector | agent | named later as |
|---|---|---|---|
| M1 | OPEN | OPEN | `EXACT` |
| M2 | DEFER | DEFER | `EXACT` |
| M3 | OPEN | **DEFER** | `UNDER_OPEN` |
| M4 | IGNORE | IGNORE | `EXACT` |

The unit PASSed, and the PASS is correct: the registered expectation for an
opened item is `("OPEN", "DEFER")`, because the declared failure is a *false
ignore* — an item the selector opens that the agent drops. A deferred item is
kept, so it is not that failure.

But `PASS` was the whole report, and M3 disappeared into it. The agent and the
deterministic selector did not agree about M3; the agent said *keep it, not
now*. That may well be the better decision — a filter that opens everything
relevant is not obviously right about *when*. The defect was not the verdict; it
was that one word covered both cases.

C3 in `SRC-008` names the four outcomes separately from that day on. **This
run's verdict stands as measured and is not regraded** — the classification
above is vocabulary applied to the stored evidence, not a new grade.

## Opaque context, measured for the first time

20 calls carry gateway token accounting. Reported `prompt_tokens` exceed the
prompt this harness sent by **2,780–2,862 tokens** on every measured call.
Something on the gateway path adds roughly 2.8k tokens of context the harness
never sees, on both routes. Every answer in every run here was produced under
instructions nobody in this repository can read. That is a limit on every
semantic conclusion drawn from this lab, and it has not changed.

## Other measurements

- `S4.ev0_is_not_weak` FAILed with `ANSWER: None`: the reply did not parse into
  the closed set. Same shape as run 2's `S4.supported_is_not_proven` — a format
  failure recorded as a semantic verdict. Both stand as measured.

## What this run does not show

Same gap as run 2: two reported models, zero observed SAIFREN members. The
population was asserted by the source file, not discovered.
