# User request

priority: P1
verify: the requested change is present and demonstrated against the user own description of it

## Request

SAIMAIL

SAIHANDOFF — SENSITIVE RECEIPT QUARANTINE / LIVE-LAB TRUTH CLASSES / STABILITY BASELINE

MODE: CONVERGE
PRIORITY: QUALITY > TIME
SCOPE: exactly 3 tightly related targets

THIS IS THE FINAL PRE-CRYPTO HARDENING WAVE.

STOP BEFORE:
- T-3 crypto
- SAIENVELOPE encryption
- SAILETTER
- FIDO/YubiKey
- Post Office persistence

======================================================================
CURRENT STATE
======================================================================

Credential handling is now substantially correct:

credential://9router/sairoute
-> local resolver
-> Windows Credential Manager
-> transport

The ordinary path does not consume an environment secret.
Provisioning is interactive/getpass only.
Historical experiment reports are preserved.
Attention outcomes distinguish EXACT / FALSE_IGNORE / UNDER_OPEN / OVER_OPEN.

The latest live run also established:

- one observed SAIFREN member
- one heterogeneous external comparator
- zero transport errors in the final narrow run
- model behavior is not stable across repeated identical scenarios

Three remaining pre-crypto issues must be closed.

======================================================================
TARGET A — SENSITIVE IMMUTABLE RECEIPT QUARANTINE
======================================================================

P0 FINDING

SRC-007 is currently:

status = ACTIVE
sensitive = false
redaction.applied = false

but its immutable body contains a historical path whose filename embeds a
credential value.

Do NOT print, quote, log, test-fixture, or reproduce the credential.

Do NOT recover other copies of it.

The human will rotate / has been instructed to rotate that credential.

The architectural problem is independent:

IMMUTABILITY MUST NOT REQUIRE NORMAL DISTRIBUTION OF SECRET PLAINTEXT FOREVER.

--------------------------------------------------
A1 — PROVE THE INCIDENT WITHOUT REPRODUCING IT
--------------------------------------------------

Create a deterministic detector/test that establishes:

SRC-007 contains sensitive credential-bearing material

without putting the credential value in:
- source code
- test output
- expected strings
- reports
- filenames
- snapshots

Use structural/pattern evidence only.

--------------------------------------------------
A2 — DEFINE RECEIPT STATES
--------------------------------------------------

Design the smallest additive lifecycle needed to distinguish:

ACTIVE_NORMAL

SENSITIVE_QUARANTINED

SANITIZED_DERIVATIVE

Exact names may differ if SAIPEN already has canonical semantics.

Do not silently mutate existing receipt meaning.

Required invariant:

ORIGINAL HASH / EXISTENCE IS PRESERVED.
SECRET PLAINTEXT IS NOT REQUIRED IN NORMAL DISTRIBUTABLE ARCHIVES.

A quarantined original may remain available only in an explicitly protected
local location if the protocol needs forensic recovery.

Normal AUDAPACK/project archives must be able to exclude the quarantined
plaintext while preserving:
- receipt id
- original hash
- reason
- sanitized derivative identity
- provenance continuity

--------------------------------------------------
A3 — SANITIZED DERIVATIVE
--------------------------------------------------

Create a sanitized derivative of SRC-007 where the credential-bearing component
is replaced by an explicit redaction marker.

Do NOT modify the original receipt in place.

The sanitized derivative must record:

original receipt id
original body hash
sanitized body hash
redaction reason/category
redaction version

The sanitized body must retain all non-secret semantic content necessary for
the associated instruction/history.

--------------------------------------------------
A4 — AUTHORITY
--------------------------------------------------

Redaction must not silently increase or decrease semantic authority.

The system must know:

this is a sanitized representation of SRC-007

not:

this is a newly authored instruction.

If redaction prevents exact interpretation of some clause, that clause becomes
UNRESOLVED rather than guessed.

--------------------------------------------------
A5 — MANIFEST / ARCHIVE POLICY
--------------------------------------------------

Define and test the distributable archive behavior.

A normal archive must not contain quarantined secret plaintext.

It may contain:
- sanitized derivative
- original hash/provenance metadata
- quarantine marker

Do not alter unrelated SAIPEN Core behavior unless absolutely necessary.

If implementing correct quarantine requires SAIPEN Core semantics outside this
repository:

STOP this subtarget after proving the requirement.

Produce:

PROTOCOL_CHANGE_PROPOSAL
EVIDENCE
REQUIRED_CORE_CHANGE
SAFE_MIGRATION_PLAN

Do not improvise around the protocol.

======================================================================
TARGET B — LIVE EXPERIMENT TRUTH CLASSES
======================================================================

CURRENT FINDING

The latest heterogeneous experiment used:

A = model observed through SAIFREN alias
B = independently selected live catalog model

B was explicitly labelled:

NOT_PROVEN_COMBO_MEMBER

Therefore the result is heterogeneous, but it does NOT prove two participants
inside the SAIFREN population.

Make this distinction first-class.

--------------------------------------------------
B1 — EXPERIMENT CLASSES
--------------------------------------------------

Define at least:

SAIFREN_INTERNAL

Requirements:
- every participant has evidence of SAIFREN membership for that experiment
- at least two distinct observable members

SAIFREN_EXTERNAL_COMPARATOR

Requirements:
- at least one observed SAIFREN participant
- one or more heterogeneous external live participants
- external participants are NOT described as SAIFREN members

SAIFREN_SINGLE_ROUTE

Requirements:
- only one observable SAIFREN member
- no cross-member claim

Do not infer internal membership from catalog eligibility.

--------------------------------------------------
B2 — REPORTING
--------------------------------------------------

Every live artifact must explicitly report:

EXPERIMENT_CLASS
COMBO
OBSERVED_COMBO_MEMBERS
EXTERNAL_COMPARATORS
DISTINCT_REPORTED_MODELS
ROSTER_STATUS

If roster is unavailable:

ROSTER_STATUS = NOT_OBSERVABLE

A sample remains a sample.

--------------------------------------------------
B3 — BOARD / DECISION LANGUAGE
--------------------------------------------------

Audit T-28 / D-020 / README / live analyses for language that may imply the
second participant was proven to belong to SAIFREN.

Do not rewrite historical evidence.

Clarify derived/current interpretation additively.

The latest run should be described as:

SAIFREN_EXTERNAL_COMPARATOR

unless stronger evidence already exists.

======================================================================
TARGET C — SEMANTIC STABILITY BASELINE
======================================================================

CURRENT FINDING

Runs 4 and 5 showed different answers from the same model for the same S3
stimulus within minutes.

Therefore:

ONE RESPONSE != MODEL PROPERTY

Add a small bounded repeatability experiment.

This is not a benchmark competition.

--------------------------------------------------
C1 — PRE-REGISTERED REPEAT SAMPLE
--------------------------------------------------

Choose only 3-4 high-value semantic cases:

1. FACTUAL + UNVERIFIED
2. EV0 attachment semantics
3. OPEN versus DEFER attention decision
4. one relation / contradiction case

For each available participant run a small fixed repeat count.

Example:
N = 3

Do not increase N after observing inconvenient results.

Keep total live-call budget bounded.

--------------------------------------------------
C2 — REPORT DISTRIBUTIONS
--------------------------------------------------

For each:

MODEL / ROUTE
SCENARIO
PROTOCOL_VERSION

report:

EXACT
UNDER_OPEN
OVER_OPEN
FALSE_IGNORE
UNANSWERED
OTHER

where applicable.

For semantic-answer scenarios use the existing deterministic semantic grader
rather than forcing attention categories.

Do not compute a universal "model quality" score.

--------------------------------------------------
C3 — PROTOCOL FRAGILITY
--------------------------------------------------

Distinguish:

MODEL_VARIANCE
different outputs from the same participant under identical declared stimulus

CROSS_MODEL_DISAGREEMENT
different participants systematically interpret differently

PROTOCOL_HOTSPOT
multiple independent participants repeatedly fail the same semantic boundary

These are not equivalent.

Do not infer motives or hidden reasoning.

--------------------------------------------------
C4 — OPAQUE CONTEXT
--------------------------------------------------

Continue recording:

LOCAL_ESTIMATED_INPUT
GATEWAY_REPORTED_INPUT
DELTA

The approximately 2.8k opaque-context observation remains a limitation.

Do not attempt to extract hidden context.

--------------------------------------------------
C5 — SAIFREN LIVE-LAB PRINCIPLE
--------------------------------------------------

Preserve the standing rule:

9router -> SAIRoute -> SAIFREN

is the canonical live SAIMAIL experimental environment.

When a proven multi-member SAIFREN roster is unavailable, external comparator
experiments remain useful but must be named honestly.

======================================================================
NON_GOALS
======================================================================

Do not implement:

- crypto
- Ed25519
- X25519
- SAIENVELOPE encryption
- FIDO/YubiKey
- SAILETTER
- human private messages
- contextual delivery
- Post Office persistence
- trust/reputation
- automatic memory promotion
- SAICHAT
- binary wire
- another ABI version
- model rankings

======================================================================
DECISION GATE
======================================================================

PASS A:
normal distributable archives can preserve source authority/provenance without
requiring the credential plaintext to travel forever.

PASS B:
no live report can confuse an external heterogeneous comparator with a proven
SAIFREN member.

PASS C:
the lab can distinguish single-response evidence from repeated semantic
behavior.

If all three pass:

NEXT_TARGET = T-4 inert-payload control review, followed by T-3 SAIENVELOPE
cryptographic envelope if T-4 closes cleanly.

Do not start them automatically.

======================================================================
FINAL REPORT
======================================================================

STATUS

SENSITIVE_RECEIPT_INCIDENT
- credential rotated: UNKNOWN / confirmed by operator only
- original plaintext handling
- sanitized derivative
- archive exclusion behavior

AUTHORITY_CONTINUITY

EXPERIMENT_CLASSES

SAIFREN_MEMBERSHIP_EVIDENCE

STABILITY_RUNS

MODEL_VARIANCE

CROSS_MODEL_DISAGREEMENT

PROTOCOL_HOTSPOTS

OPAQUE_CONTEXT

NEGATIVE_FINDINGS

PROTOCOL_CHANGE_PROPOSALS

SAIPEN_LIFECYCLE

DECISION_GATE

NEXT_TARGET

STOP.
